\ProvidesPackage{settings}

\usepackage[utf8]{vietnam} % dùng để viết tiếng Việt
\usepackage[utf8]{inputenc}
\usepackage{tipa}
\usepackage{amssymb}	% dùng để sử dụng thêm các ký hiệu toán học
\usepackage{graphicx}	% dùng để chèn hình ảnh
\usepackage{subcaption}	% dùng để chèn nhiều hình ảnh trong một hình ảnh
\usepackage{booktabs}	% dùng để tạo bảng
\usepackage{mathptmx}	% dùng để sử dụng font Times New Roman
\usepackage{amsmath}	% dùng để sử dụng thêm các ký hiệu toán học
\usepackage{amssymb}	% dùng để sử dụng thêm các ký hiệu toán học	
\usepackage{amsfonts}	% dùng để sử dụng thêm các ký hiệu toán học	
\usepackage{array}		% Cung cấp các kiểu cột bổ sung cho bảng, như cột trung tâm, cột căn giữa, và cột có chiều rộng cố định
\newcolumntype{P}[1]{>{\centering\arraybackslash}p{#1}} % Cột căn giữa
\newcolumntype{M}[1]{>{\centering\arraybackslash}m{#1}} % Cột căn giữa
\usepackage{multirow} 	% Hỗ trợ tạo hàng trong bảng
\usepackage{algorithm2e} % Hỗ trợ tạo giải thuật
% Đổi tên mặc định của algorithm thành giải thuật nếu là 1, Các giải thuật nếu nhiều, Danh sách giải thuật nếu là ds
\SetAlgorithmName{Giải thuật}{Các giải thuật}{Danh sách giải thuật}
\usepackage{hyperref} 	% Hỗ trợ tạo hyperlink trong văn bản
\usepackage{float} 		% Hỗ trợ đặt hình ảnh chính xác
\linespread{1.25} % Giãn dòng 1.25 lần
% Set paragraph spacing
% \setlength{\parskip}{\baselineskip}% Có nghĩa là khoảng cách giữa các đoạn là 1 dòng
% \setlength{\parskip}{6pt} % Điều này sẽ đặt giá trị của parskip (khoảng cách dòng giữa các đoạn văn) là 6pt
% Load the parskip package with skip and indent options
\usepackage[skip=12pt plus1pt, indent=1.25cm]{parskip} % Định dạng thụt đầu dòng và khoảng cách giữa các đoạn
\usepackage{colortbl} % Hỗ trợ tạo bảng màu, VD: \cellcolor{gray} 
\usepackage{titlesec} % Hỗ trợ tạo tiêu đề
\titleformat{\chapter}[block]{\normalfont\color{blue}\huge\bfseries\centering}{\chaptertitlename\ \thechapter}{16pt}{\Huge}
\titlespacing*{\chapter}{0pt}{\baselineskip}{\baselineskip}
\titleformat{\section}[block]{\normalfont\Large\bfseries}{\thesection}{1em}{}
\titlespacing*{\section}{0pt}{\baselineskip}{\baselineskip}
\titleformat{\subsection}[block]{\normalfont\large\bfseries}{\thesubsection}{1em}{}
\titlespacing*{\subsection}{0pt}{\baselineskip}{\baselineskip}

\usepackage{fancyhdr} 	% Hỗ trợ tạo header và footer
\fancypagestyle{header-footer}{
	\fancyhf{} % Xóa nội dung cũ trong header và footer
	\fancyfoot[C]{\thepage} % Đặt số trang trong footer ở giữa
	\renewcommand{\headrulewidth}{0pt} % Vô hiệu hóa đường kẻ phân cách header và nội dung
	\renewcommand{\footrulewidth}{0pt} % Độ dày đường kẻ phân cách footer và nội dung
}

% Tùy chỉnh hiển thị tên tác giả in đậm
% \renewcommand*{\mkbibnamefamily}[1]{\textbf{#1}}

% \usepackage{algorithm}

\usepackage{xcolor}
\usepackage{listings} % Hỗ trợ tạo khung code
\lstset{
    language=Python, % Ngôn ngữ code
    basicstyle=\fontsize{10}{12}\ttfamily, % Kích thước và font chữ cơ bản
    keywordstyle=\color{blue!60!black}, % Màu cho từ khóa
    commentstyle=\color{green!60!black}, % Màu cho comment
    stringstyle=\color{red!60!black}, % Màu cho chuỗi
    numbers=left, % Hiển thị số dòng
    % numberstyle=\tiny\color{gray}, % Kiểu số dòng
    stepnumber=1, % Bước đếm số dòng
    showstringspaces=false, % Không hiển thị dấu khoảng trắng trong chuỗi
    breaklines=true, % Tự động xuống dòng khi dòng quá dài
    breakatwhitespace=true, % Tự động xuống dòng tại khoảng trắng
    frame=single, % Viền xung quanh code
    captionpos=b % Vị trí tiêu đề của code
    escapeinside={(*@}{@*)} % Cho phép sử dụng ký tự đặc biệt trong code
}
\usepackage{picture} % Hỗ trợ vẽ hình
\usepackage{graphicx} % Hỗ trợ chèn hình ảnh
\graphicspath{ {figs/} } % Đường dẫn đến thư mục chứa hình ảnh

% \usepackage{babel} % Hỗ trợ phân loại tài liệu theo ngôn ngữ

\def\makeprintversion{} % Comment this line to make an electronic version

% Declaration
\newenvironment{declaration}{
	\begin{center}
		{\fontsize{16}{1}\selectfont \textbf{\textcolor{blue} {Lời cam đoan}} }\\[1em]
	\end{center}

	\quotation	% Đánh dấu đầu dòng
}{}
